CC=gcc
SOURCES=pre.c
OBJECTS=$(SOURCES:.c=.o)
LIBS=-lm
EXECUTABLE=mypr3
all:$(EXECUTABLE)
$(EXECUTABLE):$(OBJECTS)
$(CC) $(OBJECTS) -o $@
$(OBJECTS): $(SOURCES)
$(CC) -c $(SOURCES)
clean:
rm $(EXECUTABLE)
rm *.o
